{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceRoot}/build/lux",
            "args": ["tests/all.lux"],
            "cwd": "${workspaceRoot}"
        }
    ]
}
