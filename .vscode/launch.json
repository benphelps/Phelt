{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceRoot}/build/phelt",
            "args": ["phelt/test.ph"],
            "cwd": "${workspaceRoot}"
        }
    ]
}
