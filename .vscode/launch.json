{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceRoot}/build/phelt",
            "args": ["phelt/hashab.ph"],
            "cwd": "${workspaceRoot}"
        }
    ]
}
